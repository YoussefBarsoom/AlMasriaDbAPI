
const schedule=(type,userID)=>
{
//type all : get all my schedule for the calendar
// type [typeName]: get typeName for my schedule

if(type=='all')
{


}
//Select * from user left join typeOfTasks ON user.type =typeOfTasks.id union Select * from user right join typeOfTasks ON user.type =typeOfTasks.id 

//where userid=[userID] AND id = [type]
}